# Basic example

This directory shows the minimum files a consuming Playwright project needs. Install Lenqo in the project, copy the config to its root, and import the capture helper from a Playwright test.

```sh
npm install --save-dev lenqo @playwright/test
npx playwright test
npx lenqo serve
```
