# Basic example

This directory shows the minimum files a consuming Playwright project needs. Install Snaplogue in the project, copy the config to its root, and import the capture helper from a Playwright test.

```sh
npm install --save-dev snaplogue @playwright/test
npx playwright test
npx snaplogue serve
```
